{
  "name": "generale-game-monorepo",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "build": "bun run --filter=./packages/* build",
    "dev": "bun run --filter=./packages/* dev"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ]
}
