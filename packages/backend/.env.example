# Email Configuration
EMAIL_METHOD=smtp
EMAIL_FROM=no-reply@example.com

# SMTP Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=username
SMTP_PASS=password

# Optional HTTP proxy settings
HTTP_PROXY=http://localhost:8000

DB_FILE_NAME=.db/my.sqlite