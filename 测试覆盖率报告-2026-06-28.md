# 测试覆盖率评估报告

**日期:** 2026-06-28
**项目:** generale-vue（多人策略游戏 monorepo）
**报告范围:** 后端 (Elysia/Bun/SQLite) + 前端 (SolidJS/PixiJS) + E2E (Playwright)

---

## 整体概览

| 维度 | 源文件 | 测试文件 | 测试用例 | 覆盖率估值 |
|------|--------|----------|----------|-----------|
| **后端** | 32 文件 (~8000 行) | 16 测试文件 (~4500 行) | ~160 有效 + ~60 skip | **~25%** |
| **前端** | 92 文件 (src/) | 1 测试文件 | 4 个 bun:test 用例 | **~1%** |
| **E2E** | — | 1 场景文件 | 1 个 Playwright 场景 | **~5% 用户路径** |
| **总计** | ~124 源文件 | 18 测试文件 | ~165 有效用例 | **<15%** |

---

## 一、后端覆盖率详评

### 左侧概述

后端共 **32 个源文件**，**16 个测试文件**。按目录划分：

| 目录 | 源文件数 | 行数 | 测试文件数 | 覆盖率估值 |
|------|---------|------|-----------|-----------|
| `routes/` | 5 | 1355 | 0 | **0%** |
| `services/` | 5 | 958 | 4 | ~30%（大量 skip） |
| `db/` | 3 | 228 | 0 | **0%** |
| `middleware/` | 1 | 29 | 0 | **0%** |
| `plugins/` | 1 | 606 | 6 | ~50%（全部 mock） |
| `game/core/` | 4 | 861 | 2 | ~60% |
| `game/instance/` | 6 | 2438 | 1 | ~10%（仅 ChatInstance 有测试） |
| `game/service/` | 4 | 1316 | 3 | ~40%（全部 mock） |
| 入口/其他 | 3 | 192 | 0 | **0%** |

### 已覆盖的领域

| 模块 | 状态 | 说明 |
|------|------|------|
| `game/core/game-utils.ts` | ✅ 60% | `areAdjacent`, `validateMove`, `handleMove`, `updateGameState`, `autoJudge`, `tick`, `mask` 均有测试。50 个 randomized fuzz 测试被 skip |
| `game/core/map-gen.ts` | ⚠️ 基础 | 3 个测试验证 2/4 人地图生成的基本 tile frequency；内部函数全未单独测 |
| `plugins/websocket.ts` | ⚠️ 50% | Domain handler 注册/回调/生命周期有 6 个测试文件 × 63 用例，但全部基于 Mock 实现 (MockSubConnector/MockConnectionManager)，真实 Elysia WS 集成路径未覆盖 |
| `game/service/GameService.ts` | ⚠️ 40% | 3 个测试文件覆盖生命周期 + HTTP API + domain handler，但全部重度 mock：RoomInstance/GameInstance/generateMap 均为 mock |
| `services/userService.ts` | ✅ 70% | CRUD + 密码哈希测试较完整，缺 `updateUsername` 频率校验和 delete 错误路径 |
| `game/instance/GameChatInstance.ts` | ✅ 7 用例 | 聊天核心功能完整测试 |

### 未覆盖的领域

#### 完全零测试

| 模块 | 文件数 | 行数 | 风险 |
|------|--------|------|------|
| `routes/user.ts` | 1 | 623 | 🔴 8+ 端点（register, login, logout, change-username/password/email, forgot-password） |
| `routes/profile.ts` | 1 | 148 | 🔴 3 端点（GET/PATCH profile, avatar upload） |
| `routes/game.ts` | 1 | 203 | 🔴 4 端点（create, info, list, connect） |
| `routes/map.ts` | 1 | 268 | 🔴 9 端点（thumbnail, list, CRUD, fork, upload） |
| `routes/utils/gameListFilter.ts` | 1 | 113 | 🟡 纯函数过滤/排序/分页 |
| `mapService.ts` | 1 | 278 | 🔴 地图 CRUD + tile 文件管理全部逻辑 |
| `authPlugin.ts` | 1 | 29 | 🔴 认证插件（session 提取/验证） |
| `db/migrate.ts` | 1 | 102 | 🟡 迁移运行器、旧数据库 seal logic |
| `state-sync.ts` | 1 | 49 | 🟡 diff/patch 增量同步逻辑 |
| `connector-manager.ts` | 1 | 49 | 🟡 connector 置换逻辑 |
| `GameInfoPresenter.ts` | 1 | 154 | 🟡 游戏信息响应格式化 |
| `RoomUpdateFilter.ts` | 1 | 73 | 🟡 房间更新事件过滤 |
| `GameServiceManager.ts` | 1 | 390 | 🔴 大厅游戏列表管理、pub/sub 通知 |

#### 关键零测试大文件

```
RoomInstance.ts      1369 行  🔴 零测试 — 项目最大文件
  ├─ 玩家加入/离开
  ├─ 队伍管理 / 颜色分配
  ├─ 房主转移 / 踢人
  ├─ 房间密码
  ├─ 观战管理
  ├─ 暂停/恢复
  ├─ 开始游戏验证
  └─ 状态广播

GameInstance.ts      696 行   🔴 零测试 — 游戏核心
  ├─ Tick 循环
  ├─ 玩家/观战 connector 管理
  ├─ Surrender 处理
  ├─ 游戏结束判定
  ├─ 状态同步 (snapshot/patch)
  └─ 断开连接处理
```

### 已知问题

1. **所有测试使用 mock DB** — `vitest.setup.ts` 把 `drizzle-orm/bun-sqlite` 换成 fake in-memory store，没有真实 SQLite 的集成测试
2. **`test:game` script 指向不存在的文件** — `websocket.game-scenario.test.ts` 不存在
3. **Service 层测试大量 skip** — sessionService 3/5 skip, profileService 3/5 skip
4. **GameService 测试全部 mock 内部依赖** — 未测试真实 `startGame()` 流程（用模拟的 generateMap、RoomInstance、GameInstance）
5. **无 HTTP 路由级集成测试** — 没有任何测试启动 Elysia app 并调用真实端点

---

## 二、前端覆盖率详评

### 源代码结构

```
src/
├── api/          6 文件  — API 客户端（auth, account, game, map, profile）
├── components/  17 文件  — UI 组件（Nav, Avatar, ChatPanel, MapRender 等）
│   ├── game/     2 文件
│   ├── room/     6 文件
│   ├── roomlist/ 3 文件
│   └── map-editor/ 3 文件
├── game/        11 文件  — 核心逻辑（reducer, selector, hooks）
├── hooks/        7 文件  — 底层 hooks（auth, websocket, synced state）
├── routes/      15 文件  — 路由页面组件
├── ui/          23 文件  — UI 原语（Button, Modal, Input, Card 等）
├── utils/        3 文件  — 工具函数（playerColor, faIconGraphic, playerDisplay）
├── ws/           1 文件  — WebSocket 连接管理
└── 根目录        5 文件  — app.tsx, index.tsx, global.d.ts, testBridge.ts
```

**共 92 个生产源文件。**

### 自动化测试覆盖

| 目录 | 源文件 | 测试文件 | 覆盖率 | 说明 |
|------|--------|----------|--------|------|
| `ws/` | 1 | 1 (4 用例) | ✅ 唯一有测试的模块 | `manager.ts` — WebSocket 连接管理的核心路径 |
| `api/` | 6 | 0 | **0%** | 全部 HTTP 客户端零测试 |
| `components/` | 17 | 0 | **0%** | 含 MapRender、ChatPanel、Game 等核心组件 |
| `game/` | 11 | 0 | **0%** | 含 reducer、selector、useGameSession 等核心逻辑 |
| `hooks/` | 7 | 0 | **0%** | 含 useAuth、useWebsocket、useSyncedState |
| `routes/` | 15 | 0 | **0%** | 含 login、room、profile、maps 等页面 |
| `ui/` | 23 | 0 | **0%** | 全部 UI 原语 |
| `utils/` | 3 | 0 | **0%** | 全部工具函数 |

**总体：1/92 文件有自动化测试（~1%）。**

### 手动测试占位符

5 个 `TestXxx.tsx` 文件放在 `__tests__/` 目录下，但**不是自动化测试**：

| 文件 | 位置 | 实际用途 |
|------|------|----------|
| `MapRenderTest.tsx` | `components/__tests__/` | 手动验证 PixiJS 地图渲染 |
| `TestPreGameRoomStateFrom.tsx` | `components/room/__tests__/` | 手动验证游戏设置表单 |
| `TestPreGameMapSettingForm.tsx` | `components/room/__tests__/` | 手动验证地图配置表单 |
| `TestPreGameControls.tsx` | `components/room/__tests__/` | 手动验证游戏控制按钮 |
| `TestPlayerList.tsx` | `components/room/__tests__/` | 手动验证玩家列表 |

这些文件**不含任何 `describe`/`it`/`expect`**，不是自动化测试，也不被任何 test runner 调用。

### 前端问题

1. 无 test runner 配置文件（无 vitest/jest config）
2. 无 setup/teardown hooks、无全局 mock、无 coverage 配置
3. 依赖 `bun test` 内置 runner，未安装任何 test library
4. 高 ROI 的纯函数（`gameReducer`, `pregameReducer`, `selectors`, 所有 `utils/`）零测试

---

## 三、E2E 覆盖率

### 基础设施

```
e2e/
├── playwright.config.ts   — 120s timeout, 1 worker, chromium
├── src/
│   ├── backend.ts          — spawn 真实 Bun 后端进程
│   ├── frontend.ts         — spawn 真实 rsbuild 前端
│   ├── testScenario.ts     — 编排完整生命周期
│   └── playerSession.ts    — DOM 优先的玩家操作 API
└── tests/
    └── game-flow.test.ts   — 唯一场景
```

### 覆盖的流程

1. 4 用户通过登录表单登录
2. Alice 创建房间
3. Bob, Charlie, Diana 加入房间
4. 验证 4 人在玩家列表中
5. 非房主准备 (ready)
6. 房主开始游戏
7. 所有玩家等待游戏状态变为 PLAYING
8. Game HUD 可见
9. 3 人投降 (Alice, Bob, Charlie)
10. 验证最后一个投降者和获胜者均看到游戏结束界面

### 未覆盖的关键路径

| 路径 | 优先级 | 建议 |
|------|--------|------|
| 注册流程 | 🔴 高 | 目前用户预 seed；需测试注册 → 验证 → 登录 |
| 邮箱验证 / 密码重置 | 🔴 高 | 整个邮件流未覆盖 |
| 地图编辑器 CRUD | 🔴 高 | 创建/编辑/发布/删除地图 |
| 个人资料编辑 | 🟡 中 | 修改展示名、bio、头像 |
| 聊天功能 | 🟡 中 | `PlayerSession.sendChat` 存在但从未被调用 |
| 游戏内 tile 交互 | 🔴 高 | 移动军队、攻击、占领 — 核心游戏玩法 |
| 房间管理 | 🟡 中 | 踢人、房主转移、房间密码 |
| 断线重连 | 🔴 高 | WebSocket 断开后恢复会话 |
| 不同地图类型 | 🟡 中 | 随机 vs 自定义 vs 导入 |
| 观战模式 | 🟡 低 | 观战者加入/离开 |

---

## 四、总体风险评估

```
高风险区（零测试，核心逻辑）：
  ── 后端路由层 (30+ API endpoints)
  ── RoomInstance (1369 行) + GameInstance (696 行)
  ── 前端 game/ 模块 (reducer, selector, hooks)
  ── 前端 api/ 客户端 (全部 HTTP 调用)

中风险区（有部分测试但缺口大）：
  ── GameService (mock heavy，未测真实流程)
  ── map-gen (仅 3 个基础测试)
  ── 后端 service 层 (profileService 5 中 3 skip)
  ── 前端 ws/ 连接管理 (4 个测试完整但单薄)
  ── E2E (仅 1 个 happy-path 场景)

低风险区（配置/类型声明）：
  ── db/schema.ts (纯声明)
  ── 前端 ui/ 组件 (变化频繁，UI 测试 ROI 较低)
```

---

## 五、改进建议（按优先级排序）

### P0 — 必须立即处理

**1. 后端 HTTP 路由集成测试**
当前所有 API 端点零测试。用 Elysia `app.handle()` 直接测试，无需启动真实服务器。
```
优先级: 最高
建议: 至少覆盖
  - POST /api/register + POST /api/login 主路径
  - GET/POST /api/game/* CRUD 主路径
  - GET /api/profile/:userId
  - 认证失败场景（未登录访问受保护路由）
```

**2. RoomInstance + GameInstance 单元测试**
项目最大两个类（合计 2065 行），0 测试。所有游戏逻辑的核心。
```
优先级: 最高
建议:
  - RoomInstance: 玩家加入/离开、房主转移、踢人、状态广播
  - GameInstance: tick 循环、surrender、addPlayer/addSpectator、断开处理
  - 先写关键路径，再补边缘情况
```

### P1 — 高强度建议

**3. 真实 DB 集成测试**
当前 vitest.setup.ts 把 SQLite 完全 mock 掉，测试无法发现数据库相关问题。
```
建议: 少量关键测试用临时 SQLite 文件（:memory:），覆盖 schema 约束和迁移
```

**4. 前端 game/ 模块单元测试**
`gameReducer`, `pregameReducer`, `selectors` 全是纯函数，极适合单元测试。
```
建议: 从 reducers 开始，30-50 个测试用例可覆盖绝大多数状态转换
```

**5. 补充 E2E 场景**
目前仅 1 个 happy-path 场景。
```
建议:
  - 注册 → 验证 → 登录 场景
  - 双人游戏内交互（移动军队、攻击）场景
  - 踢人/房主转移场景
  - 断线重连场景
```

### P2 — 质量提升

**6. 修复 `test:game` 脚本**
`package.json` 中 `test:game` 指向 `src/plugins/__tests__/websocket.game-scenario.test.ts`，该文件不存在。

**7. 取消或补全 skip 的测试**
- profileService: 3/5 skip
- sessionService: 3/5 skip
- emailService: 1/3 skip
- game/core: 50 randomized fuzz test skip

**8. 统一测试配置**
- 前端增加 vitest 配置（支持 coverage 报告）
- 消除对 `bun test` 内置 runner 的单一依赖

**9. 处理 5 个伪测试文件**
`TestXxx.tsx` 放在 `__tests__/` 但无断言 → 要么转为自动化测试，要么移出 `__tests__/` 目录。

---

## 六、测试文件清单

### 后端测试 (16 文件)

| 测试文件 | 测试对象 | 用例数 | 状态 |
|----------|---------|--------|------|
| `services/__tests__/userService.test.ts` | UserService | 8 | ✅ |
| `services/__tests__/sessionService.test.ts` | SessionService | 5 (3 skip) | ⚠️ |
| `services/__tests__/profileService.test.ts` | ProfileService | 5 (3 skip) | ⚠️ |
| `services/__tests__/emailService.test.ts` | EmailService | 3 (1 skip) | ⚠️ |
| `plugins/__tests__/websocket.unit.test.ts` | WS domain handlers | 15 | ✅ |
| `plugins/__tests__/websocket.basic.test.ts` | WS basic lifecycle | 9 | ✅ |
| `plugins/__tests__/websocket.domain-handlers.test.ts` | WS domain lifecycle | 7 | ✅ |
| `plugins/__tests__/websocket.functional.test.ts` | WS functional | 14 | ✅ |
| `plugins/__tests__/websocket.realtime.test.ts` | WS real-time | 8 | ✅ |
| `plugins/__tests__/websocket.multi-dev-reconnection.test.ts` | WS reconnection | 10 | ✅ |
| `game/core/game.test.ts` | Core game logic | ~35 | ✅ |
| `game/core/map-gen.test.ts` | Map generation | 3 | ⚠️ |
| `game/instance/__tests__/GameChatInstance.test.ts` | Chat instance | 7 | ✅ |
| `game/service/__tests__/GameService.lifecycle.test.ts` | GameService lifecycle | 9 (mock) | ⚠️ |
| `game/service/__tests__/GameService.httpApi.test.ts` | GameService HTTP API | 14 (7 skip, mock) | ⚠️ |
| `game/service/__tests__/GameService.domainHandlers.test.ts` | GameService handlers | 11 (mock) | ⚠️ |

### 前端测试 (1 文件)

| 测试文件 | 测试对象 | 用例数 | 状态 |
|----------|---------|--------|------|
| `ws/__test__/manager.test.ts` | ClientConnectionManager | 4 | ✅ |

### E2E 测试 (1 文件)

| 测试文件 | 场景 | 用例数 | 状态 |
|----------|------|--------|------|
| `tests/game-flow.test.ts` | 4 玩家全流程 → 投降 | 1 | ✅ |

---

*报告由 opencode 自动生成。*
